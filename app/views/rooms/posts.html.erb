<div class="container-fiuld room-posts">
  <h4 class="pb-4">登録済みルーム一覧</h4>
  <table>
    <thead>
      <tr align="center">
        <th>ルーム画像</th>
        <th>ルーム名</th>
        <th>ルーム紹介</th>
        <th>料金</th>
        <th>作成日</th>
      </tr>
    </thead>
    <tbody>
        <% @rooms.each do |room| %>
        <tr>
            <td><%= image_tag room.image.variant(resize:'200x250').processed %></td>
            <!--以下まだ作成できていないのでログインpathに飛ばしているが、showに飛ばす。-->
            <td><%= link_to room.name, room, class:"posts-name" %></td>
            <td class="posts-content"><%= room.introduction %></td>
            <td class="price-icon"><%= room.price %></td>
            <td><%= room.created_at.to_s(:datetime_jp) %></td>
        </tr>
        <% end %>
    </tbody>
  </table>
</div>
